name: "CTT"

on:
  push:
    branches:
    - "master"

jobs:
  ctt-test:
    name: "Test"
    runs-on: "ubuntu-latest"
    steps:
    - uses: "actions/checkout@v4"
    - name: "Install Poetry"
      run: "pipx install poetry"
    - uses: "actions/setup-python@v5"
      with:
        cache: "poetry"
    - name: "Install Redis"
      run: "sudo apt install redis"
    - name: "Run CTT"
      run: "poetry install && ctt"
